from re import A


def FPB (x, y) :
        if x >= y :
            a = y
        elif x < y :
            a = x
        for i in range (1, a+1) : 
                if x%i == 0 and y%i == 0 :
                    fpb = i 
        return fpb
try : 
    x = (int (input()))
    y = (int (input()))
    fpb = FPB (x, y)
    print (fpb)
except : 
    print ("Input Tidak Valid")

