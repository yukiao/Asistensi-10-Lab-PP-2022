def faktorial (x, y) :
    x = x-1
    if x != 0 : 
        y = x*y
        faktorial(x, y) 
    elif x == 0 : 
        print (y)
x = (input())
if x.isnumeric() :
    x = int(x) 
    y = x
    faktorial(x, y) 
else :
    print ("Input Harus Berupa Angka")